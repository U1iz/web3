function call() {} {
    let dir = 'src/content/';

    LM('阿b日志v2');

    img_fn(dir + 'tree.png', '70%');
    space();
    ajax_hlc(dir + 'b_v2_d1.js', '阿b日志v2 demo0.1 v2.0', fn_01());

    function fn_01() {
        space();
    }
}